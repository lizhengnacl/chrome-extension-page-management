# AGENTS.md

## 项目概览

**项目名称**: 页集 (TabStack)  
**项目类型**: Chrome 浏览器扩展程序  
**技术栈**: React 18 + TypeScript + Tailwind CSS + NextUI + Rsbuild

## 项目简介

「页集」是一个轻量级、支持多维度组织的网页收藏与管理工具，旨在替代浏览器原生书签。其核心价值在于通过"分组"实现一键打开特定场景下的所有相关页面，并通过"多级标签"进行细致的内容管理。

## AI 代理配置

### 核心工作流

本项目采用标准化的四阶段开发流程，通过自定义技能实现从想法到代码的完整转化：

```
idea → spec → plan → tasks → exec
```

### 可用技能

#### 1. `/spec` - 需求澄清与规格定义
- **触发词**: "我有个想法"、"澄清需求"、"挖掘边缘场景"
- **职责**: 将模糊想法转化为详细的 spec.md 文档
- **输出**: specs/{编号}-{功能描述}/spec.md

#### 2. `/plan` - 技术方案设计
- **触发词**: "制定技术方案"、"设计技术方案"
- **职责**: 基于 spec.md 生成技术实现方案 plan.md
- **输出**: specs/{编号}-{功能描述}/plan.md

#### 3. `/tasks` - 任务拆解
- **触发词**: "拆解技术方案"、"任务分解"
- **职责**: 将技术方案分解为原子化、可执行的任务列表
- **输出**: specs/{编号}-{功能描述}/tasks.md
- **特点**: 严格遵循 TDD 理念，测试任务先行

#### 4. `/exec` - 任务执行
- **职责**: 基于 tasks.md 执行开发任务
- **流程**: 红灯（失败测试）→ 绿灯（通过代码）→ 重构
- **要求**: 严格按照阶段和依赖顺序执行

#### 5. `/idea` - 全流程自动化
- **触发词**: /idea
- **职责**: 依次调用 spec → plan → tasks → exec，实现从想法到代码的完整流水线
- **特点**: 自动化执行，无需手动触发中间步骤

## 开发规范

### 文件结构
```
specs/
└── {编号}-{功能描述}/
    ├── spec.md      # 需求规格
    ├── plan.md      # 技术方案
    └── tasks.md     # 任务列表
```

### 核心原则
1. **TDD 优先**: 测试先行，红灯-绿灯-重构
2. **包内聚**: 模块设计遵循高内聚低耦合
3. **错误处理**: 完善的异常捕获和用户友好的错误提示
4. **渐进式开发**: 分阶段交付，每个阶段都有可验证的成果

## 常用命令

```bash
# 安装依赖
pnpm install

# 开发模式
pnpm dev

# 构建项目
pnpm build

# 预览构建结果
pnpm preview

# 生成图标
pnpm generate-icons
```

## 技术栈详情

- **UI 框架**: React 18
- **类型安全**: TypeScript
- **样式方案**: Tailwind CSS
- **组件库**: NextUI
- **构建工具**: Rsbuild
- **拖拽功能**: @dnd-kit
- **图标库**: lucide-react
- **动画**: framer-motion

## Chrome 扩展权限

- `storage`: 数据存储与同步
- `tabs`: 标签页管理
- `bookmarks`: 书签导入导出
- `notifications`: 通知提示

## 快捷键

- `Ctrl+Shift+S` (Windows) / `Command+Shift+S` (Mac): 快速打开收藏面板

## 项目维护者

本项目使用 AI 辅助开发，遵循上述工作流程进行功能迭代和维护。
